# ── Virtual environments ──
venv_pipeline/
venv_training/
venv/

# ── Python cache ──
__pycache__/
*.pyc
*.pyo
.env

# ── Model files (too large for GitHub) ──
*.keras
*.tflite

# ── Training artifacts ──
processed_general/

# ── Dataset images (too large for GitHub) ──
AI model data/

# ── Pipeline runtime folders ──
pipeline/test_images/
pipeline/processed_images/

# ── Log data files (keep the logger script, not the CSV data) ──
pipeline/logs/*.csv

# ── OS / IDE ──
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
